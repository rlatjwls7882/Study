#include<bits/stdc++.h>
using namespace std;

int main(void) {
    ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);\
    int a, b; cin >> a >> b;
    if(a/2==b || a==b/2) cout << "Yes";
    else cout << "No";
}