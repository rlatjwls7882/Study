#include <bits/stdc++.h>

using namespace std;

int main(void) {
    ios::sync_with_stdio; cin.tie(); cout.tie();
    int N; cin >> N;

    if(N==1) {
        cout << "Hello World";
    } else {
        int A, B; cin >> A >> B;
        cout << A+B;
    }
}