#include <bits/stdc++.h>

using namespace std;

int main(void) {
    ios::sync_with_stdio(); cin.tie(); cout.tie();
    long long N; cin >> N;

    if(N%2==1) {
        cout << "SK";
    } else {
        cout << "CY";
    }
}